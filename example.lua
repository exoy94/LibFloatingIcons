--[[ Library Handler ]]

-- you need to register a library handler

local name = "lfiExample_position"
local LFI = LibFloatingIcons:RegisterHandler( name )  

-- the name has to be unique within all handlers created by addons or other librarys 
-- it should indicate, by which addon the handler is created: 


-- purpose of the handler: 

-- you can define handler specific default settings, render settings and such  
-- enables traceability, which addon has created which icons 


-- you can have create multiple handler within one addon, but they need a unique name 


--[[ Position Icons - Explanation ]]

-- 


